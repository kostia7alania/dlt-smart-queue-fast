node_modules
dist
build
vendor
.next
main

# Local dev-server output
*.log

# Editor/assistant tooling (kept local, not part of the repo contract).
# The spec-driven workflow itself lives in plain Markdown under docs/ and specs/.
.specify/
.windsurf/
.claude/
.zenflow/
.zencoder/
