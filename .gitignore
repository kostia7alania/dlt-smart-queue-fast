node_modules
dist
build
vendor
.next
main

# Local dev-server output
*.log

# Editor/assistant tooling (kept local, not part of the repo contract).
# The spec-driven workflow itself lives in plain Markdown under docs/ and specs/.
.specify/
.windsurf/
.zenflow/
.zencoder/

# Claude Code: shared project config (.claude/launch.json, .claude/settings.json)
# is committed; only personal overrides stay local.
.claude/settings.local.json
