# Evals

To evaluate the product:
1. Ensure the monorepo boots locally using `make up`, `make api-dev`, `make web-dev`.
2. Check that the UI successfully connects to the API via BFF.
3. Validate OpenAPI docs at `/docs`.
