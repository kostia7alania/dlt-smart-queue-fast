# golangci-lint v2 configuration (https://golangci-lint.run)
version: "2"

linters:
  default: standard
  enable:
    - misspell
    - unconvert
    - unparam

formatters:
  enable:
    - gofumpt
    - goimports
